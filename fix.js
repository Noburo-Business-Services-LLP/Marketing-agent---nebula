
const fs = require("fs");
let code = fs.readFileSync("frontend/pages/ReelGenerator.tsx", "utf8");

// 1. step2Next
code = code.replace(
  "      videoStyle,\n      preserveIdentity,\n      characterUsage,\n      characterConsistencyStrength,\n      currentStep: 3\n    });\n  });",
  "      videoStyle,\n      location,\n      preserveIdentity,\n      characterUsage,\n      characterConsistencyStrength,\n      currentStep: 3\n    });\n    setStep(3);\n  });"
);

// 2. startAutoGenerate
code = code.replace(
  "      videoStyle,\n      preserveIdentity,\n      characterUsage,\n      characterConsistencyStrength,\n      useLogo\n    };",
  "      videoStyle,\n      location,\n      preserveIdentity,\n      characterUsage,\n      characterConsistencyStrength,\n      useLogo\n    };"
);

// 3. handleSaveCharacter
code = code.replace(
  "        artStyle: characterArtStyle,\n        videoStyle: videoStyle,\n        appearance: characterAppearance,\n        characterImageBase64: originalCharacterImage\n      });",
  "        artStyle: characterArtStyle,\n        videoStyle: videoStyle,\n        location: location,\n        appearance: characterAppearance,\n        characterImageBase64: originalCharacterImage\n      });"
);

// 4. generatePromptAndScenes
code = code.replace(
  "  const generatePromptAndScenes = async () => withBusy(async () => {\n    if (!jobId) throw new Error(\"Draft missing. Complete step 1 first.\");\n\n    // Use the new Video Style-Aware Prompt Engineering system\n    const response = await videoGenerationAPI.generateVideoStylePrompts({\n      description: description,\n      videoStyle: videoStyle || \"Cinematic Commercial\",\n      characterName: characterName || \"Character\",\n      sceneCount: sceneCount || 5,\n      productName: selectedProduct?.name || undefined,\n      duration: durationSeconds || 30\n    });\n\n    if (!response?.success) {\n      throw new Error(response?.message || \"Style-aware scene generation failed\");\n    }\n\n    setScenes(response.scenes || []);\n    if (response.promptText) {\n      setPromptText(response.promptText);\n    }\n    \n    // Save generated scenes to the draft immediately so they persist\n    const saveResponse = await videoGenerationAPI.generateScenes({\n      jobId,\n      sceneData: response.scenes,\n      saveOnly: true\n    });\n    \n    // Save promptText to the draft as well\n    if (response.promptText) {\n      await videoGenerationAPI.generatePrompt({\n        jobId,\n        promptText: response.promptText,\n        saveOnly: true\n      });\n    }\n    \n    setDraft(saveResponse?.draft || draft);\n  });",
  "  const generatePromptAndScenes = async () => withBusy(async () => {\n    if (!jobId) throw new Error(\"Draft missing. Complete step 1 first.\");\n\n    const sceneResponse = await videoGenerationAPI.generateScenes({\n      jobId\n    });\n\n    setScenes(sceneResponse?.sceneData || []);\n    setDraft(sceneResponse?.draft || draft);\n  });"
);

// 5. handleUploadMedia
code = code.replace(
  "        characterName,\n        videoStyle,\n        useLogo\n      };\n      console.log(\"?? Sending payload to backend:\", payload);",
  "        characterName,\n        videoStyle,\n        location,\n        useLogo\n      };\n      console.log(\"?? Sending payload to backend:\", payload);"
);

// 6. UI Replacement
const searchUI = `                        <div>\n                          <label className={\`text-xs font-bold uppercase tracking-wide \${theme.textMuted}\`}>Image Prompt</label>\n                          <textarea value={scene.imagePrompt || ""} onChange={(e) => setScenes((prev) => prev.map((item, i) => i === idx ? { ...item, imagePrompt: e.target.value } : item))} className={\`\${inputClass} mt-1 min-h-[130px] max-h-[300px] overflow-y-auto resize-y\`} placeholder="Describe the visual for the still image" disabled={isRegen} />\n                        </div>\n\n                        <div>\n                          <label className={\`text-xs font-bold uppercase tracking-wide \${theme.textMuted}\`}>Video Prompt</label>\n                          <textarea value={scene.videoPrompt || ""} onChange={(e) => setScenes((prev) => prev.map((item, i) => i === idx ? { ...item, videoPrompt: e.target.value } : item))} className={\`\${inputClass} mt-1 min-h-[130px] max-h-[300px] overflow-y-auto resize-y\`} placeholder="Describe the motion / animation for the clip" disabled={isRegen} />\n                        </div>`;
const replaceUI = `                        <div className="grid grid-cols-2 gap-4 mt-2">\n                          <div>\n                            <label className={\`text-xs font-bold uppercase tracking-wide \${theme.textMuted}\`}>Purpose & Emotion</label>\n                            <input value={scene.purpose || ""} onChange={(e) => setScenes((prev) => prev.map((item, i) => i === idx ? { ...item, purpose: e.target.value } : item))} className={\`\${inputClass} mt-1 mb-2\`} placeholder="Purpose" disabled={isRegen} />\n                            <input value={scene.emotion || ""} onChange={(e) => setScenes((prev) => prev.map((item, i) => i === idx ? { ...item, emotion: e.target.value } : item))} className={\`\${inputClass}\`} placeholder="Emotion" disabled={isRegen} />\n                          </div>\n                          <div>\n                            <label className={\`text-xs font-bold uppercase tracking-wide \${theme.textMuted}\`}>Cinematography</label>\n                            <input value={scene.camera || ""} onChange={(e) => setScenes((prev) => prev.map((item, i) => i === idx ? { ...item, camera: e.target.value } : item))} className={\`\${inputClass} mt-1 mb-2\`} placeholder="Camera (e.g. Medium Shot, Tracking)" disabled={isRegen} />\n                            <input value={scene.lighting || ""} onChange={(e) => setScenes((prev) => prev.map((item, i) => i === idx ? { ...item, lighting: e.target.value } : item))} className={\`\${inputClass}\`} placeholder="Lighting (e.g. Golden hour)" disabled={isRegen} />\n                          </div>\n                        </div>\n\n                        <div className="grid grid-cols-3 gap-4 mt-2">\n                          <div>\n                            <label className={\`text-xs font-bold uppercase tracking-wide \${theme.textMuted}\`}>Location ID</label>\n                            <input value={scene.locationId || scene.location || ""} onChange={(e) => setScenes((prev) => prev.map((item, i) => i === idx ? { ...item, locationId: e.target.value } : item))} className={\`\${inputClass} mt-1\`} placeholder="e.g. LOC_001" disabled={isRegen} />\n                          </div>\n                          <div>\n                            <label className={\`text-xs font-bold uppercase tracking-wide \${theme.textMuted}\`}>Character IDs</label>\n                            <input value={Array.isArray(scene.characterIds) ? scene.characterIds.join(", ") : scene.characterIds || ""} onChange={(e) => setScenes((prev) => prev.map((item, i) => i === idx ? { ...item, characterIds: e.target.value.split(",").map(s => s.trim()).filter(Boolean) } : item))} className={\`\${inputClass} mt-1\`} placeholder="e.g. CH_001" disabled={isRegen} />\n                          </div>\n                          <div>\n                            <label className={\`text-xs font-bold uppercase tracking-wide \${theme.textMuted}\`}>Wardrobe IDs</label>\n                            <input value={Array.isArray(scene.wardrobeIds) ? scene.wardrobeIds.join(", ") : scene.wardrobeIds || ""} onChange={(e) => setScenes((prev) => prev.map((item, i) => i === idx ? { ...item, wardrobeIds: e.target.value.split(",").map(s => s.trim()).filter(Boolean) } : item))} className={\`\${inputClass} mt-1\`} placeholder="e.g. WD_001" disabled={isRegen} />\n                          </div>\n                        </div>\n\n                        <div>\n                          <label className={\`text-xs font-bold uppercase tracking-wide \${theme.textMuted}\`}>Cinematic Action</label>\n                          <textarea value={scene.action || ""} onChange={(e) => setScenes((prev) => prev.map((item, i) => i === idx ? { ...item, action: e.target.value } : item))} className={\`\${inputClass} mt-1 min-h-[80px]\`} placeholder="Describe the action in the scene" disabled={isRegen} />\n                        </div>`;
code = code.replace(searchUI, replaceUI);

fs.writeFileSync("frontend/pages/ReelGenerator.tsx", code);

